#!/usr/bin/python3
import sys
from socket import *
import Dzombie.py *
class CreateClient(ocject):
      def OpenTcpSocket():
        serverName = 'Zserver.py;
        serverPort = 8080
        #create socket for client with network type and tcp socket specification
        clientSocket = socket(AF_INET, SOCK_STREAM)
        #initiate tcp connection, completes last step for tcp connection
        sock.connect((m.getHost(), m.getPort())) 
        sock.send(msg)
    return
#read the request from server
class Message(object):
    def pareseMessage():
        if(sys.argv[1] == "-d"):
            url = sys.argv[2]
            script = sys.argv[3]
            m_report = True
        else:
            url = sys.argv[1]
            script = sys.argv[2]
            m_report = False
        
        o = urlparse(url)
        return o
    #takes messafe and return HTTP request
    def buildmessage(self):
        msg = "GET " + self.o.path + " HTTP/1.1\r\nHost: " + self.o.hostname + "\r\nUser-Agent: Wget/1.19.4 (linux-gnu)\r\nConnection: close\r\n\r\n"
        return bytes(msg, "utf-8")
    #return server hostname in sting formate, takes url
    def getServerHost(self, urlparsed):
        return self.urlparsed.hostname
    def getServerPort(self, urlparsed):
        if(self.urlparsed== None):
            print("ERROR: No port detected")
            break
        else:
            return self.urlparsed.port
    def getScriptName(self, port):
        self.port=port
        self.
        zombie= Dzombie.main(i,j)
        for i in range(len(sripts.py)):
            if i==self.script:
                //SetRequestType(self, i)?
            else:
                print("Error: Python script doesn't exist in this zombie")
                break
    def SetRequestType(self, i):
        self.i =i
        if requestType= 'RUN':
              #run specidied zombie and script
            Dzombie.main(i)
        
class Response(object):
    def bufferMessages(self. sock):
        http_response= ""
        script = ""
        status = ""
        while True:
            packet =socket(recv)
  

def main()
    def OpenTcpSocket()

main()

